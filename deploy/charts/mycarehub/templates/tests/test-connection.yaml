apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mycarehub.fullname" . }}-test-connection"
  labels:
    {{- include "mycarehub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mycarehub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
